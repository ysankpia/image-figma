# Legacy Pencil Go Experiment

This directory is retained as a superseded Go Pencil export/server experiment.

It is not the current Pencil product delivery route on `main`. Current Pencil delivery is owned by:

```text
services/pencil-python-backend/
```

Rules:

- Do not revive this service as the default product path.
- Do not add new assisted slice features here.
- Keep it only as implementation reference unless a new active plan explicitly resumes it.

See:

```text
docs/engineering/legacy-code-inventory.md
docs/engineering/current-code-map.md
```
