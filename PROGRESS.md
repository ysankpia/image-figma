# Progress

This file is the live execution ledger for Image-to-Figma Design. It does not replace `docs/roadmap.md`, active plans, bug records, or validation docs.

## Current objective
Continue plan 189: move Slice Studio toward a multi-user production SaaS without regressing the current Slice Studio export workflow.

## Active plan
- Prior text/slice coordination plan still active for closeout context: `docs/plans/active/193-pencil-export-text-slice-coordination.md`
- Production launch plan active: `docs/plans/active/189-slice-studio-multi-user-production-launch.md`
- Most recently completed:
  - `docs/plans/completed/195-current-psdlike-text-style-service.md`
  - `docs/plans/completed/194-psdlike-text-style-evidence-gate.md`
  - `docs/plans/completed/191-repository-legacy-code-physical-archive.md`
  - `docs/plans/completed/192-promote-slice-studio-to-repository-root.md`

## Current phase
Slice Studio multi-user production launch implementation

## Now
- 2026-06-17: advanced plan 189 admin payment operations. `/admin` now surfaces recent payment orders and payment events; admin-only API routes can inspect payment ops state and manually mark repairable pending/failed orders as paid. Manual repair rejects non-admin, already-paid, closed, and refunded orders; it grants entitlement through the same local plan path and writes a `manual_mark_paid` payment event. Remaining payment work is provider order query, reconciliation, refund/cancel handling, and broader admin user/plan management.
- 2026-06-17: advanced plan 189 entitlement gates beyond AI/export. Project creation now checks free/paid project-count limits; page upload checks per-project page count and account original-image storage; page replacement checks projected storage after replacing the current original. `/api/me` now returns `accountUsage`, and `/billing` displays project count, page count, and used storage. Validation passed: `pnpm run typecheck`, `bun run smoke:auth-billing`, `pnpm run smoke`, `pnpm run check`, `pnpm run build`, and `git diff --check`.
- 2026-06-17: advanced plan 189 payment fulfillment from pending-order skeleton to a minimal XPay / 易支付 adapter. `POST /api/billing/orders` can generate an XPay checkout URL when provider env vars are set; `POST /api/billing/webhooks/xpay` verifies MD5 callback signatures, records `payment_events`, marks verified success callbacks as paid, and grants the local plan entitlement. Forged callbacks are recorded but do not grant entitlement. Remaining payment work is production reconciliation, refund/cancel states, provider order query, and admin repair operations.
- 2026-06-17: current 189 stage 1 verification passed on the real worktree. The local SQLite migration now rebuilds stale `usage_events` and `payment_events` tables instead of only adding columns, `bun run smoke:auth-billing` passes, `pnpm run smoke` passes against the current owner session, `pnpm run check` and `pnpm run build` pass, `git diff --check` passes, and browser smoke on `/login`, `/projects`, `/billing`, and `/admin` passes with anonymous `/projects` redirecting to `/login`.
- 2026-06-17: Plan 189 Stage 1 foundation advanced. Slice Studio now has landing/login surfaces, server-backed email/password sessions, repeatable bootstrap local admin, project ownership, protected project APIs/downloads, protected Next `/projects` and review entries, `/settings`, `/billing`, `/admin` shells, entitlement/usage tables, AI/export quota consumption, provider-neutral pending XPay order skeleton, same-origin `/api` browser proxy, and auth/billing Bun smoke coverage. Remaining 189 work is verified XPay/webhook fulfillment, richer admin/user/plan operations, production DB/storage, legal/help/deploy/backup docs, and final real-flow validation.
- 2026-06-17: starting Plan 189 stage 1 implementation. Scope is the production identity/ownership foundation: session entry, project ownership, protected project APIs/downloads, basic account usage/billing/admin shell stubs, and provider-neutral payment/entitlement skeletons. Existing `next-env.d.ts` generated diff is pre-existing and will stay out of task-scoped commits unless it becomes necessary.
- 2026-06-17: CodeGraph audit confirmed the current Slice Studio mainline still only exposes /, /projects, and /projects/:projectId/review, with an unauthenticated Elysia API and SQLite schema limited to projects/pages/slices. The formal SaaS launch gap now explicitly covers landing, pricing, login/register, account/settings, usage/credits, billing/orders, payment checkout/result, help/legal, admin dashboard, user/plan/payment management, audit logs, production storage, ownership, and entitlement gates.
- 2026-06-17: Reference repo /Users/luhui/pencil.2.figma shows a mature payment/quota/admin shape that can inform Slice Studio, but it is not the current product. XPay / 易支付 / 码支付 style provider docs at https://x.yhhrun.cn/doc/epay_submit look usable as the first payment adapter if Slice Studio keeps the local order/entitlement truth source, verifies callbacks server-side, and logs raw payment events.
- 2026-06-14: fixed the P9 payment-detail remainder/text ghosting regression in the current no-vector export path. Root cause was twofold: normal text knockouts discarded the already-known foreground text color and fell back to background-difference detection, while `paintTextForeground()` replaced glyph pixels with one estimated background color and left antialias/shadow residue. The render plan now passes foreground color for all text knockouts; text knockout bounds cover the union of OCR and physical boxes; `pencil-package` builds a dilated glyph mask and locally inpaints masked pixels instead of doing single-color repaint. PSD-like color output is now contrast-guarded against local background so low-contrast sampled background colors do not replace clear local foreground colors.
- 2026-06-14: fixed raster-owned control-label text ghosting after the no-vector strategy. Slice Studio still does not emit OCR-owned filled control surfaces as Pencil vector rectangles, but filled button/status labels now get a clipped glyph-only text knockout: the owner surface provides the rounded clip, the label color provides the foreground target, and the local background is estimated after excluding glyph-colored pixels. This removes the original raster text under the editable overlay without repainting button edges or flattening gradients.
- 2026-06-14: corrected the P10/P11 button protrusion root cause. The visible green artifacts were not source button pixels that failed to clear; pixel comparison showed Slice Studio had repainted originally white pixels green during text knockout padding for control-surface labels. The render plan now omits text foreground repaint for editable text that already owns an exported filled control surface, so the control surface shape and editable text become the sole owners of that region.
- 2026-06-14: converged the plan 193 button protrusion fix into an explicit page render/ownership plan instead of leaving it as another `pencil-package` heuristic. Gemini discussion identified the main contradiction as fuzzy source subtraction vs formal vector reconstruction. Slice Studio now builds a small `PageRenderPlan` from already-computed text layers and saved slices; the plan owns control-surface z-order and `SurfaceKnockout` instructions, while `pencil-package` only executes those instructions.
- 2026-06-14: fixing the remaining P10/P11 button protrusion as a PSD-like layer ownership bug, not as OCR/text threshold tuning. The target is `remainder` cleanup for exported control surfaces: the vector/control surface keeps its precise visible bbox, while the source raster layer relinquishes the matching source-surface pixels in a small geometry-derived drift band.
- 2026-06-14: starting a regression baseline investigation for the Pencil multi-layer text/control export issue. Current dirty working tree is not treated as baseline truth. The check will compare clean isolated worktrees for `cbac554` (before plan 193) and `85fac96` (plan 193 commit) against the same `project_mqc1wpkd_123c88b0` storage data, then inspect exported manifest/Pencil package layer ownership before recommending rollback or repair direction.
- Plan 195 is complete. Slice Studio now has a current `services/psdlike-text-style/` service and a fail-open TypeScript client. Normal runtime defaults to `SLICE_STUDIO_TEXT_STYLE_PROVIDER=psdlike`; test runtime defaults to `fallback` unless explicitly overridden. Editable text reconstruction keeps OCR/M29/slice ownership in TypeScript and uses the service only for font size, weight, family, color, alignment, and measured glyph bounds.
- Plan 194 is complete as an evidence gate, not a production integration. The archived PSD-like measurement endpoint proved the direction on the real P10 `搜索` defect: current manifest has `fontSize=40` and `fontWeight=600`, while PSD-like measurement returns `fontSize=31`, `fontWeight=500`, `color=#fcfdfc`, and centered control-surface text. This confirms the next owning layer is font/style measurement, not OCR content, slice ownership, or Pencil layer order.
- Plan 193 is being tightened with the selected compact text render box contract. The remaining P10 `搜索` problem was traced to `.pen` text nodes using expanded `safeBBox` as their visible fixed layout box: the control surface was `116x59`, but the text node became `109x75` and shifted upward. `TextLayer` now separates `placement`, `textRenderBBox`, `safeBBox`, and `knockoutBBox`; exported Pencil text nodes use `textRenderBBox` while `safeBBox` remains metadata/audit evidence.
- Plan 193 now has a validated fix for the nested search/button control-surface defect: old PSD-like/Codia behavior showed the missing abstraction was OCR-anchored control surface ownership, not another remainder-only patch. The TypeScript path now samples control fill from support pixels around text, trims thin connected outline strokes to the dense filled control core, requires real control padding before treating a text-adjacent component as a filled owner surface, preserves owner corner radius, emits non-background filled owner surfaces as Pencil rectangles below editable text, and keeps saved slices above editable text as final visible owners.
- The concrete P10 `搜索` failure is now fixed from the source of truth. Before this pass, the manifest selected the outer white search field / green outline as the owner and inflated the label to `fontSize=55`. The current export selects the inner green button owner at roughly `x=773 y=161 width=116 height=59`, fill `#0eb12f`, corner radius `27`, uses white text color, and uses `fontSize=40`.
- Full-project Pencil validation for `project_mqc1wpkd_123c88b0` has now been rerun after backend restart. The current full export `/private/tmp/slice-full-current-fix/package/design.pen` opens in Pencil with 28 frames and `snapshot_layout(problemsOnly=true)` reports `No layout problems`. Visual screenshots for `page_0010__frame` and `page_0006__frame` show no reported button square/protrusion, oversized text spill, or icon/text overlap main fault. Remaining imperfections are OCR/content-quality and naturally co-owned icon/text regions, not the nested control-surface root cause.
- Repository root is now the Slice Studio product app: `app/`, `components/`, `server/`, `shared/`, `scripts/`, and `tests/`.
- Legacy/reference source code lives under `archive/legacy-code/`; archived runtime artifacts remain ignored and archived packages are not part of the active workspace.
- Local Slice Studio data now lives under root `storage/`; current observed project directory count is `17`.
- Root `.env.local` carries Slice Studio local keys; the previous root env was backed up to an ignored `.env.local.legacy-backup-*` file.
- Plan 189 remains the formal multi-user launch plan for auth/session, ownership, production database/storage, entitlement, payment, and deployment.

## Done
- 2026-06-14: validated the P9 fix on a restarted current-code dev stack. Full export for `project_mqc1wpkd_123c88b0` returned `assetCount=532`, `pageCount=28`. New package `/private/tmp/slice-studio-p9-after/project/design.pen` opened in Pencil with `snapshot_layout(problemsOnly=true)` reporting `No layout problems`; `page_0009__frame` screenshot showed clean white `待支付` and bottom `立即支付 ￥149.00` without the previous visible text ghosting. Pixel scan on P9 key text regions reported `stillFgRatio=0` for `待支付`, the long payment warning, both `服务已完成` labels, the orange/white `￥149.00` labels, and `立即支付`. `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 49 tests; `pnpm run check` passed with 9 files / 94 tests; `pnpm run build` and `git diff --check` passed.
- 2026-06-14: validated the text-ghosting fix on `project_mqc1wpkd_123c88b0`. P16 `查看订单` original white glyph pixels inside the knockout region dropped from `white=381, bright=589` in the source to `white=0, bright=0` in the exported remainder. Page exports for P10/P11/P15/P16 and the full 28-page export passed; the full package had 532 assets, 1330 text layers, and `0` `slice_studio_control_surface` nodes. Pencil `snapshot_layout(problemsOnly=true)` reported `No layout problems`. `pnpm run check`, `pnpm run build`, and `git diff --check` passed.
- 2026-06-14: validated the no-vector control-surface strategy on `project_mqc1wpkd_123c88b0`. Page exports for P10/P11/P15/P16 passed; the full 28-page export produced 532 assets, 1330 text layers, and `0` `slice_studio_control_surface` nodes. Pencil `snapshot_layout(problemsOnly=true)` reported `No layout problems` for the full package and targeted P10/P11/P15/P16 frames. `pnpm run check`, `pnpm run build`, and `git diff --check` passed.
- 2026-06-14: fixed the self-inflicted green control-surface smear in Pencil remainder generation. `buildPageRenderPlan()` now excludes text knockouts for text layers that have a non-background filled owner surface, because the raster source remains the visual owner for that control and the editable text layer is only an overlay. Regression coverage asserts that filled-control text does not create a text knockout, while ordinary/background text still does.
- 2026-06-14: added `server/render-plan.ts` and `server/render-plan-builder.ts` as a minimal LayerOwnershipPlan for Pencil export. The builder does not run OCR, M29, or project reads; it only converts current `TextLayer` owner metadata and saved slices into remainder text knockouts, control-surface surface knockouts, and explicit layer z-order. `server/pencil-exporter.ts` now consumes that plan for control surfaces, text, slices, and remainder generation. `server/pencil-package.ts` now executes `SurfaceKnockout` instructions instead of deriving control-surface drift/fill ownership itself.
- 2026-06-14: completed plan 195. Added focused current Python service `services/psdlike-text-style/`, `/health`, `/api/text-style-batch`, TypeScript client `server/psdlike-text-style.ts`, text-style provider config, per-page style batch wiring in `server/text-reconstruction.ts`, manifest audit fields `textStyleSource/textStyleMeasured`, dev script `pnpm run dev:text-style`, and docs/env updates. Current runtime no longer needs to run PSD-like code from `archive/legacy-code`.
- 2026-06-14: completed plan 194 as a PSD-like text-style evidence gate and opened plan 195 for the real current-service implementation. Validation used the real current project source image `storage/projects/project_mqc1wpkd_123c88b0/originals/page_0010.png` and the P10 `搜索` owner surface. Current manifest exports `page_0010__text_0005` as `fontSize=40`, `fontWeight=600`; PSD-like measurement returns `fontSize=31`, `fontWeight=500`, `fontFamily=PingFang SC`, `color=#fcfdfc`, measured `62x34`. Manually entered bbox samples for other labels were not accepted as hard evidence because color sampling depended on exact owner-surface coordinates.
- 2026-06-14: selected and implemented the compact text render bbox contract for plan 193. `TextLayer` now carries `textRenderBBox`; `pencil-exporter` writes `.pen` text node `x/y/width/height` from `textRenderBBox` instead of expanded `safeBBox`; manifest output records `textRenderBBox`; Pencil package validation now checks editable text node bounds against `metadata.textRenderBBox`. P10 `搜索` changed from the previous `.pen` text node `x=785 y=153 width=109 height=75` to `x=785 y=170 width=92 height=42`, while the green owner surface remains `x=773 y=161 width=116 height=59`.
- 2026-06-14: fixed the nested search-button/control-surface root cause by porting the old OCR-seeded surface behavior instead of tuning knockout padding. `detectSeededFilledControlSurface()` now finds the filled control core from OCR text seed color, trims thin connected outline strokes using dense row/column support, rejects tight text foreground as a control surface unless there is real padding, avoids re-centering already accurate `local_foreground` text, and samples text color against the filled owner surface. P10 `搜索` now owns the inner green button, not the outer search field.
- 2026-06-14: fixed the remaining P10/P11 button surface issue by migrating the old OCR-anchored control-surface contract into the TypeScript Pencil exporter. `textOwnerSurface` fill sampling now uses support pixels around the text instead of the search edge, non-background filled owner surfaces are emitted as rounded Pencil rectangles between the remainder and editable text, owner text is center-aligned, the `.pen` contract validates that those rectangles exist below the text, and manifest text layers now record owner-surface metadata for audit.
- 2026-06-13: fixed the remaining rounded-button protrusion in Pencil remainder generation. Text knockout now estimates the local dominant background and repaints only foreground glyph pixels inside `knockoutBBox`, instead of flattening the whole rectangle. P10 `查看详情` validation showed only `1323 / 4070` pixels changed inside the knockout box and only `18 / 1100` changed in the top edge band, removing the rectangular button-edge artifact.
- 2026-06-13: fixed rounded-button damage in Pencil remainder generation. Text layers now carry a separate `knockoutBBox` for erasing original glyph pixels, while `safeBBox` remains only the Pencil text layout box. This prevents expanded safe text bounds from repainting rounded button corners into rectangular color blocks, as seen on P10 `查看详情`.
- 2026-06-13: promoted the page 6 slice-aware local foreground rule into a general overlap rule. Text candidates whose OCR bbox or current physical bbox intersects confirmed slice ownership now attempt blocker-aware foreground measurement even when the incoming bbox source is `m29_foreground` or pure OCR fallback. Regression tests now cover left, right, top, and bottom icon positions plus over-broad M29 icon+text foreground.
- 2026-06-13: corrected Slice Studio Pencil editable text contract for page 6: final render/knockout source defaults to OCR/original text bbox, M29/local foreground remains physical/color evidence, outlined surfaces no longer squeeze text, solid surfaces may center/fix short labels, and `.pen` text nodes use safe fixed bounds instead of `textGrowth:auto`. Pencil MCP on a cache-busted exported page 6 document reported `No layout problems`.
- 2026-06-13: corrected the remaining page 6 icon/text overlap and oversized-font issue: local foreground measurement now masks confirmed slice-owned pixels before computing the editable text bbox, and font fitting uses physical glyph bounds instead of the broader OCR line/control box when physical evidence exists.
- 2026-06-13: added page-scoped Pencil export: `POST /api/projects/:projectId/pages/:pageId/export-project` and `GET /api/projects/:projectId/pages/:pageId/project.zip`, plus a Review Workbench `当前页包 / Current page` button.
- 2026-06-13: ran old PSD-like/Codia oracle for page 6 with the same Baidu PP-OCRv5 evidence: output had `text=53`, `raster=16`, `shape=14`, no missing assets, and a visually clean `draft_preview.png`. The useful behavior is measured/region-based text replacement; the old `mediaOwnedText` suppression made `完成服务` visually clean by leaving it raster-owned, but that conflicts with the current requirement that uncut text should be editable.
- 2026-06-13: plan 193 root-cause check against old Go M29 and PSD-like 109 found that the remaining Pencil export issue is text owner style/placement, not a generic layout failure: OCR owns text content, M29/local foreground owns physical bbox evidence, local owner surfaces may cap and position short control labels, and confirmed slices remain the visible owner above text.
- 2026-06-13: restored OCR-backed TS M29 lineage by passing OCR blocks into `m29-physical-evidence`, emitting OCR-source `text_region` primitives and a text mask while keeping those OCR regions out of physical bbox matching.
- 2026-06-13: fixed owner-surface false positives in Slice Studio text reconstruction: a filled candidate must now have real geometry outside the text bbox or strong outline evidence before it can cap font size or shift text placement. Page 6 evidence shows `订单详情` no longer receives `textOwnerSurface`, while button labels such as `联系用户`, `拒单`, `商认上门`, and `完成服务` still get owner-aware treatment.
- 2026-06-13: plan 193 investigation checked the historical M29/OCR ownership path: plan 179 made M29 the physical bbox authority, plan 180 intentionally switched Slice Studio text nodes to `textGrowth:auto`, and the old Pencil Python backend kept OCR visibility as an explicit handoff mode. Current fix keeps M29/OCR editable text instead of hiding it, while tightening the Pencil frame contract.
- 2026-06-13: completed plan 192 and moved it to `docs/plans/completed/192-promote-slice-studio-to-repository-root.md`.
- 2026-06-13: completed plan 191 and moved it to `docs/plans/completed/191-repository-legacy-code-physical-archive.md`.
- 2026-06-13: physically archived legacy/reference code under `archive/legacy-code/`, added archive recovery rules, and removed the empty root `tools/` directory.
- 2026-06-13: promoted Slice Studio from `apps/slice-studio/` to the repository root; root scripts now run Slice Studio directly.
- 2026-06-13: moved local Slice Studio runtime data to root `storage/`; browser/API validation confirmed `17` projects still list.
- 2026-06-13: completed plan 190 and moved it to `docs/plans/completed/190-slice-studio-prelaunch-codebase-hardening.md`.
- 2026-06-13: backed up Slice Studio local storage to `backups/slice-studio-storage-20260613-023319` with `projects=17`, `pages=47`, `slices=643`, size `223M`.
- 2026-06-13: added root Slice Studio scripts, marker READMEs for legacy/reference directories, backup/release/local smoke runbooks, and OpenRouter/OpenAI-compatible chat-completions AI provider support.
- 2026-06-12: completed Review Workbench i18n plan 188 and moved it to `docs/plans/completed/188-review-workbench-i18n.md`.
- 2026-06-12: added component-local Chinese/English dictionary, `sliceStudio.reviewLanguage.v1` persistence, and a topbar language switch in `ReviewWorkbenchClient.tsx`.
- 2026-06-12: localized visible Review Workbench labels, status messages, progress text, modal copy, placeholders, and aria labels while leaving project names, file names, asset names, API enum values, and export contracts unchanged.
- 2026-06-12: stabilized command button widths and text overflow in `globals.css` so Chinese/English labels do not reshape the toolbar.
- 2026-06-12: changed Asset Overview grid to fixed five columns per row.
- 2026-06-12: moved Select/Draw/Pan from the top command bar into a Pencil-style floating canvas tool rail on the left side of the stage.
- 2026-06-12: fixed Review Workbench inspector height allocation so Assets keeps a stable reserved region and Details scrolls independently instead of pushing asset rows out of view.
- 2026-06-12: fixed Review Workbench zoom/layout regressions: top command bar remains one visible row at narrow/zoomed widths, canvas tool buttons moved to the left of the command bar, canvas floating page info card removed, duplicate Overview button removed from Assets, and Asset Overview cards no longer stretch when there are few assets.
- 2026-06-12: completed plan 187 and moved it to `docs/plans/completed/187-slice-studio-figma-workbench-implementation.md`.
- 2026-06-12: implemented Figma node `607:207` in `ReviewWorkbenchClient.tsx` as a real workbench: command bar, Pages rail, canvas meta/footer, Assets panel, Details panel, AI progress panel, independent Assets collapse, and full inspector collapse.
- 2026-06-12: added adaptive CSS for the Review Workbench using `clamp()`, component `container-type`, named `@container` rules, `:has()` state styling, fluid grid columns, and container-sized panel density.
- 2026-06-12: preserved existing upload, page list, page reorder, Konva select/draw/pan, manual box create/move/resize, asset rename, bbox editing, crop mode, delete, overview, AI current/all pages, and export controls.
- 2026-06-12: implemented Slice Studio text ownership policy `slice_studio_text_ownership.v1`.
- 2026-06-12: added M29/local foreground over-broad bbox fallback before Pencil font sizing.
- 2026-06-12: added regression tests for over-broad M29 boxes and generated marker labels.
- 2026-06-12: re-exported `project_mqar9qpo_93b911d9`; dense PC page changed from 208 visible text layers to 116 visible text layers plus 103 raster-preserved text decisions.
- 2026-06-12: inspected regenerated `design.pen` with Pencil MCP; large OCR text pollution was removed from the dense PC page.
- 2026-06-12: moved plan 185 to completed and bug 024 to resolved.
- 2026-06-12: analyzed dense PC UI failure against current Slice Studio code and historical clean-editable / visual-fidelity / visual-ocr contracts.
- 2026-06-12: created active plan 185 for a scoped text ownership arbiter.
- 2026-06-12: confirmed the current code and completed plans identify `apps/slice-studio` as the working product surface.
- 2026-06-12: confirmed existing uncommitted changes in `apps/slice-studio/server/ai-slice-boxes/provider.ts`, `apps/slice-studio/tests/ai-slice-boxes.test.ts`, and `docs/reference/slice-studio-ai-slice-prompt-strategies.md`; these are treated as pre-existing work.
- 2026-06-12: added direction contract and progress ledger.
- 2026-06-12: updated root README, AGENTS, docs index, roadmap, product docs, architecture docs, engineering docs, runbooks, env vars, bug/reference entries, code map, and legacy inventory to route default product work to Slice Studio.
- 2026-06-12: moved plan 184 to completed.

## Next
- Continue plan 189 with auth/session and project ownership.
- Confirm the first production auth contract: Better Auth as the session/user identity boundary, Chinese default language with browser-language auto selection, and Google login only when provider credentials are configured.
- Keep archived legacy code under `archive/legacy-code/` unless a new active plan revives a specific component.
- Run a real OpenRouter provider smoke after the user provides a key and model.

## Blocked or deferred
- Figma shows persistent page processing states, asset lock state, and redo behavior. These are not currently backed by persisted Slice Studio contracts and must be treated as missing interfaces unless implemented later.
- Figma-style asset semantic categories beyond `cutMode` are not currently backed by `SliceRecord`; filtering remains limited to existing crop-mode data.
- Crop super-resolution / UpscalerJS is deferred; current evidence does not justify adding it to the mainline dependency path.
- Source images that already contain blue detection boxes/labels still preserve those pixels as raster; this fix prevents double-emitting them as visible OCR text layers.

## Validation log
- 2026-06-17: plan 189 admin payment ops validation passed. `pnpm run typecheck` passed; `bun run smoke:auth-billing` passed and now covers admin-only manual paid-order repair plus `manual_mark_paid` event logging; `pnpm run smoke` passed with `{"ok":true,"projectId":"project_mqi570di_5069e282","assetCount":1,"pages":["P1-订单页","P2-首页"],"projectZip":true}`; `pnpm run check` passed with 9 test files / 97 tests; `pnpm run build` passed; `git diff --check` passed; restarted Web/API dev processes on 3010/4110 and verified `/api/admin/payments` plus `/admin` render the payment ops sections.
- 2026-06-17: plan 189 minimal XPay fulfillment validation passed. `pnpm run typecheck` passed; `bun run smoke:auth-billing` passed and now covers configured XPay checkout URL generation, forged signature rejection, verified success webhook fulfillment, and entitlement upgrade to the paid plan; `pnpm run check` passed with 9 test files / 97 tests; `pnpm run build` passed; `git diff --check` passed.
- 2026-06-17: plan 189 stage 1 verification passed on the current worktree. `pnpm run typecheck` passed; `bun run smoke:auth-billing` passed; `pnpm run smoke` passed with `{"ok":true,"projectId":"project_mqi1usvz_6e6e3fb3","assetCount":1,"pages":["P1-订单页","P2-首页"],"projectZip":true}`; `pnpm run check` passed with 9 test files / 97 tests; `pnpm run build` passed; `git diff --check` passed; browser smoke using system Chrome passed with anonymous `/projects` redirecting to `/login`, authenticated `/projects` showing local projects, and `/billing` plus `/admin` rendering.
- 2026-06-14: plan 193 control-surface text knockout root-fix validation passed. `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 42 tests; `pnpm run typecheck` passed; `pnpm run check` passed with 9 test files / 87 tests; `pnpm run build` passed; `git diff --check` passed. Direct current-code page exports for `project_mqc1wpkd_123c88b0/page_0010` and `page_0011` returned `assetCount=30` and `31`. Pixel comparison proved the old artifact was self-inflicted: original P10 `x=748,y=597` was `[254,254,254,255]`, the broken remainder had `[18,184,51,255]`, and the fixed remainder is back to `[254,254,254,255]`; actual button interior `x=760,y=600` is `[10,173,38,0]` so the vector surface owns it. Full export returned `assetCount=532,pageCount=28`; unpacked `/private/tmp/slice-193-rootfix-full/design.pen` has 28 frames, and Pencil MCP screenshots for `page_0010__frame` and `page_0011__frame` showed no visible green button protrusion.
- 2026-06-14: plan-driven control-surface ownership validation passed. `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 41 tests; `pnpm run check` passed with 9 test files / 86 tests; `pnpm run build` passed; `git diff --check` passed. Real page exports for `project_mqc1wpkd_123c88b0/page_0010` and `page_0011` both returned ok. Pixel audit on unpacked `/private/tmp/slice-plan-p10` and `/private/tmp/slice-plan-p11` found `coloredSurfaceCount=5,totalSurviving=0` for the top drift-band source-fill residuals on each page. Pencil MCP opened both generated `.pen` files; `snapshot_layout(problemsOnly=true,maxDepth=4)` reported `No layout problems`, and screenshots of `page_0010__frame` and `page_0011__frame` showed no visible green button top protrusion.
- 2026-06-14: plan 195 validation passed. `cd services/psdlike-text-style && uv run pytest -q` passed with 3 tests; `pnpm run check` passed with 9 test files / 82 tests; `pnpm run build` completed successfully; `git diff --check` passed. With text-style service running on `127.0.0.1:4120` and API restarted on `127.0.0.1:4110`, page-scoped export for `project_mqc1wpkd_123c88b0/page_0010` returned `assetCount=30,pageCount=1`; manifest shows `page_0010__text_0005` (`搜索`) as `fontSize=31`, `fontWeight=500`, `textStyleSource=psdlike`, `textStyleMeasured={width:62,height:34}`, `textRenderBBox={x:796,y:174,width:70,height:33}`. With the service stopped, the same page export still succeeded and recorded `textStyleSource=fallback`, old `fontSize=40`, `fontWeight=600`. Full export returned `assetCount=532,pageCount=28`; full manifest had `textStyleSource` counts `psdlike=1330`. Pencil MCP opened `/private/tmp/slice-full-psdlike/package/design.pen`; `snapshot_layout(problemsOnly=true,maxDepth=3)` reported `No layout problems`, and `page_0010__frame` screenshot showed the search button label at reduced measured size.
- 2026-06-14: plan 194 evidence-gate validation passed. `cd archive/legacy-code/services/psdlike-python && uv run pytest tests/test_text_style_api.py -q` passed with 2 tests. Real P10 sample call to `/api/text-style-batch` against `storage/projects/project_mqc1wpkd_123c88b0/originals/page_0010.png` returned `fontSize=31`, `fontWeight=500`, `color=#fcfdfc`, measured `62x34`, elapsed `0.026s`. Existing export manifest from `storage/projects/project_mqc1wpkd_123c88b0/exports/project.zip` shows the same `搜索` label currently at `fontSize=40`, `fontWeight=600`, proving the PSD-like path targets the failing style variable.
- 2026-06-14: plan 193 compact text render bbox validation passed. `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 35 tests; `pnpm exec vitest run tests/m29-physical-evidence.test.ts tests/pencil-exporter.test.ts` passed with 42 tests; `pnpm run typecheck` passed; `pnpm run check` passed with 8 files / 77 tests; `pnpm run build` completed successfully; `git diff --check` passed. Direct current-code page export for `project_mqc1wpkd_123c88b0/page_0010` returned `assetCount=30,pageCount=1`; unpacked `/private/tmp/slice-page10-text-renderbbox/design.pen` shows `page_0010__text_0005` (`搜索`) exported at `x=785,y=170,width=92,height=42`, with owner surface `x=773,y=161,width=116,height=59`. Full current-code export returned `assetCount=532,pageCount=28`; unpacked `/private/tmp/slice-full-text-renderbbox/design.pen` has the same compact P10 text node and Pencil MCP `snapshot_layout(problemsOnly=true,maxDepth=3)` reported `No layout problems`. Pencil MCP screenshot for `page_0010__frame` showed the search button without the previous oversized safe text box.
- 2026-06-14: restarted Slice Studio API after the compact text render bbox fix. Old `bun` PID `56474` on `127.0.0.1:4110` was stopped; `bun run dev:api` is now running current code and `GET /api/health` returns `{"ok":true}`.
- 2026-06-14: plan 193 nested-control final validation passed after API restart. `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 35 tests; `pnpm exec vitest run tests/m29-physical-evidence.test.ts` passed with 7 tests; `pnpm run check` passed with 8 files / 77 tests; `pnpm run build` completed successfully; `git diff --check` passed. API PID `8871` was killed and restarted with current code. `POST /api/projects/project_mqc1wpkd_123c88b0/pages/page_0010/export-project` returned `assetCount=30`, `pageCount=1`; full export returned `assetCount=532`, `pageCount=28`. Pencil MCP opened `/private/tmp/slice-page10-current-fix/package/design.pen` and `/private/tmp/slice-full-current-fix/package/design.pen`; both returned `snapshot_layout(problemsOnly=true): No layout problems`. Full-export screenshots for `page_0010__frame` and `page_0006__frame` showed the reported square-button/text-overlap main fault resolved.
- 2026-06-14: plan 193 OCR-anchored control-surface validation passed. `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 34 tests; `pnpm exec tsc --noEmit` passed. Existing running API exported `/private/tmp/slice-page10-control-surface/design.pen`; Pencil MCP opened it, `snapshot_layout(problemsOnly=true)` returned `No layout problems`, and `get_screenshot(page_0010__frame)` showed `查看详情/立即预约` green capsule buttons without the previous rectangular protrusion. Full export `/private/tmp/slice-full-export-control-surface-1781367105/design.pen` returned `assetCount=532`, `pageCount=28`; Pencil MCP opened it, `snapshot_layout(problemsOnly=true)` returned `No layout problems`, and visual screenshots for `page_0006__frame`, `page_0010__frame`, and `page_0011__frame` showed the icon/text and green-button cases no longer exhibiting the reported layout/shape failure.
- 2026-06-13: plan 193 glyph-only text knockout validation passed. `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 33 tests; `pnpm exec tsc --noEmit` passed; `pnpm run check` passed with 8 files / 75 tests; `pnpm run build` completed successfully; `git diff --check` passed. API PID 75916 was killed and restarted with current code; `POST /api/projects/project_mqc1wpkd_123c88b0/pages/page_0010/export-project` returned `assetCount=30`, `pageCount=1` in about 3.4s. New package `/private/tmp/slice-page10-glyph-knockout/design.pen` opened in Pencil and `snapshot_layout(problemsOnly=true)` returned `No layout problems`. New crop `/private/tmp/slice-button-edge-analysis-glyph/02-remainder-3x.png` showed the button remainder no longer has the hard rectangular top protrusion. Full export also passed: `POST /api/projects/project_mqc1wpkd_123c88b0/export-project` returned `assetCount=532`, `pageCount=28`; `/private/tmp/slice-full-export-glyph-knockout/design.pen` opened in Pencil and `snapshot_layout(problemsOnly=true)` returned `No layout problems`.
- 2026-06-13: plan 193 text knockout validation passed: `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 32 tests; `pnpm run check` passed with 8 files / 74 tests; `pnpm run build` completed successfully; `git diff --check` passed.
- 2026-06-13: plan 193 P10 button remainder validation passed after backend restart. `POST /api/projects/project_mqc1wpkd_123c88b0/pages/page_0010/export-project` returned `assetCount=30`, `pageCount=1` in about 3.3s. `/private/tmp/slice-page10-knockout-fixed/assets/visible/remainders/P10/remainder.png` crop showed the `查看详情` button remained rounded after text knockout. Full export also passed: `POST /api/projects/project_mqc1wpkd_123c88b0/export-project` returned `assetCount=532`, `pageCount=28` in about 2m06s. `/private/tmp/slice-full-export-knockout-fixed-1781362382/design.pen` opened in Pencil and `snapshot_layout(problemsOnly=true)` returned `No layout problems`; P10 screenshot showed the `查看详情` button no longer has the rectangular green overflow.
- 2026-06-13: plan 193 generalized slice-aware overlap validation passed: `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 31 tests; `pnpm run check` passed with 8 files / 73 tests; `pnpm run build` completed successfully; `git diff --check` passed.
- 2026-06-13: plan 193 real full export after backend restart passed: API PID 56009 was killed and restarted with current code; `POST /api/projects/project_mqc1wpkd_123c88b0/export-project` returned `assetCount=532`, `pageCount=28` in about 2m09s. New cache-busted package `/private/tmp/slice-full-export-overlap-1781359903/design.pen` opened in Pencil with 28 frames and `snapshot_layout(problemsOnly=true)` returned `No layout problems`. Manifest coordinate audit improved from old `pagesWithAnyOverlap=19,total=73,bigTotal=69` to new `pagesWithAnyOverlap=7,total=11,bigTotal=9`. Remaining intersections are mostly rating stars, certification/badge symbols, bottom/navigation labels, and illustration text.
- 2026-06-13: plan 193 Pencil MCP screenshots on the new full export checked `page_0005__frame`, `page_0006__frame`, `page_0010__frame`, `page_0020__frame`, `page_0023__frame`, and `page_0024__frame`; no new whole-page layout collapse, cross-frame text spill, or large visible icon/text overpainting was observed.
- 2026-06-13: plan 193 full Pencil review passed structurally but not as a perfect layer-geometry closeout. Opened `/private/tmp/slice-full-export-fixed-1781354661/design.pen` with Pencil MCP; editor state showed 28 top-level frames and `snapshot_layout(problemsOnly=true, maxDepth=3)` returned `No layout problems`. `get_screenshot` was run for all 28 frames. Visual review found no whole-page collapse, cross-frame spill, or large icon/text overpainting. Manifest coordinate audit still found text/slice placement intersections on 19 pages; examples are mostly expected icon+text/rating/navigation co-owned UI groups, but pages 13/14/15 retain OCR/content-quality defects that should not be confused with the page 6 geometry fix.
- 2026-06-13: plan 193 post-safe-fixed-bounds validation passed: `pnpm run check` passed with 8 test files / 67 tests; `pnpm run build` completed successfully; `git diff --check` produced no output.
- 2026-06-13: plan 193 final icon/text coordination validation passed: `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 26 tests; `pnpm run check` passed with 8 test files / 68 tests; `pnpm run build` completed successfully; `git diff --check` produced no output.
- 2026-06-13: plan 193 page 6 re-export after backend restart passed: `POST /api/projects/project_mqc1wpkd_123c88b0/pages/page_0006/export-project` returned `assetCount=23`, `pageCount=1`; manifest reported `textLayerCount=54`, `rasterPreservedTextCount=0`, `skippedTextCount=0`. Geometry spot check showed target icon-adjacent labels (`联系用户`, `联系人：小雨`, `联系电话`, `服务地址`, `距客户`, `服务时长`, `订单编号`, `拒单`, `商认上门`) had zero overlap with confirmed icon slices and smaller physical-glyph-based font sizes.
- 2026-06-13: plan 193 Pencil MCP validation passed on cache-busted `/tmp/slice-page6-export-fixed-1781354298/design.pen`: `snapshot_layout(problemsOnly=true)` returned `No layout problems`; screenshot showed labels no longer overlapping icons and the remaining `商认上门` issue is OCR text recognition, not layout geometry.
- 2026-06-13: plan 193 full export smoke passed after final text geometry fix: `POST /api/projects/project_mqc1wpkd_123c88b0/export-project` returned `assetCount=532`, `pageCount=28` in about 52 seconds.
- 2026-06-13: plan 193 page-scoped export smoke passed on restarted current-code services: `POST /api/projects/project_mqc1wpkd_123c88b0/pages/page_0006/export-project` returned `assetCount=23`, `pageCount=1`, `url=/api/projects/project_mqc1wpkd_123c88b0/pages/page_0006/project.zip`; `HEAD` for that zip returned HTTP 200.
- 2026-06-13: plan 193 full export smoke passed on restarted current-code services: `POST /api/projects/project_mqc1wpkd_123c88b0/export-project` returned `assetCount=532`, `pageCount=28`, `url=/api/projects/project_mqc1wpkd_123c88b0/project.zip` in about 68 seconds; `HEAD` for that zip returned HTTP 200.
- 2026-06-13: plan 193 Pencil MCP validation passed on cache-busted `/tmp/slice-page6-export-fixed-1781352626/design.pen`: `snapshot_layout(problemsOnly=true)` returned `No layout problems`; `page_0006__frame` screenshot showed clean editable text replacement without the prior cross-frame text spill, large layout boxes, or white button knockout blocks. Manifest reported `sourceLineCount=54`, `textLayerCount=54`, `rasterPreservedTextCount=0`, and `skippedTextCount=0`.
- 2026-06-13: plan 193 browser smoke passed on `http://127.0.0.1:3010/projects/project_mqc1wpkd_123c88b0/review`: Review Workbench loaded 28 pages and showed `资产包`, `当前页包`, and `项目包` controls from the restarted current-code web/API services.
- 2026-06-13: plan 193 focused validation passed after owner-surface tightening: `pnpm exec vitest run tests/pencil-exporter.test.ts tests/m29-physical-evidence.test.ts` passed with 31 tests; `pnpm run check` passed with 8 test files / 66 tests; `pnpm run build` completed successfully.
- 2026-06-13: plan 193 page 6 evidence rerun on `project_mqc1wpkd_123c88b0` after code changes: `订单详情` rendered from local foreground at `fontSize=23.8` with no owner surface; control labels retained owner surfaces, including `联系用户`, `复制`, `拒单`, `商认上门`, and `完成服务`. Final export validation still requires backend restart before regenerating `project.zip`.
- 2026-06-13: plan 193 validation passed: `pnpm exec vitest run tests/pencil-exporter.test.ts` passed with 22 tests; `pnpm run check` passed with 8 test files / 63 tests; `pnpm run build` completed successfully; `git diff --check` produced no output.
- 2026-06-13: plan 193 real export validation passed for `project_mqc1wpkd_123c88b0`: direct `exportPencilProject` returned `assetCount=532`, `pageCount=28`; regenerated `storage/projects/project_mqc1wpkd_123c88b0/exports/project.zip`; unpacked `design.pen` had 28 frames and `badClipFrames=0`; Pencil MCP `snapshot_layout` reported `No layout problems`; page `page_0006__frame` screenshot rendered without the global-canvas spill seen in the user screenshot.
- 2026-06-13: plan 191/192 validation passed: `pnpm install` completed; `pnpm run check` passed with 8 test files / 60 tests; `pnpm run build` completed successfully; `pnpm run smoke` created and deleted temporary project `project_mqbju82i_bf09eab6` and exported both `assets.zip` and `project.zip`.
- 2026-06-13: plan 191/192 completion audit passed after repository-root promotion: `pnpm run check` passed with 8 test files / 60 tests; `pnpm run build` completed successfully; `pnpm run smoke` created temporary project `project_mqbku312_d343ee48` and exported `project.zip`; targeted current-doc grep found no stale `apps/slice-studio` mainline commands or active-path references.
- 2026-06-13: plan 191/192 browser smoke passed on `http://127.0.0.1:3010/projects`: API returned `17` projects, UI showed `共 17 个项目。`, source-image preview requests returned 200, and console had no errors.
- 2026-06-13: plan 191/192 browser smoke passed on `http://127.0.0.1:3010/projects/project_mqavhwm7_875518fe/review`: source image loaded at `1672x941`, 7 asset previews loaded, Konva canvas rendered at `649x769`, network requests returned 200, and console had no errors.
- 2026-06-13: plan 191/192 artifact discipline check passed: `git diff --check` produced no output; `git ls-files` runtime-artifact grep only matched `storage/.gitkeep` plus archived source paths whose names contain `storage`.
- 2026-06-13: plan 190 completion validation passed: `pnpm --dir apps/slice-studio run check` passed with 8 test files / 60 tests; `pnpm --dir apps/slice-studio run build` passed; `pnpm -r run check` passed across Slice Studio, packages, services, and figma-plugin.
- 2026-06-13: plan 190 API smoke passed with `pnpm --dir apps/slice-studio run smoke`: created temporary project `project_mqba2sqe_67812ca4`, uploaded pages, saved slices, exported `assets.zip`, exported `project.zip/design.pen`, and deleted the temporary project.
- 2026-06-13: plan 190 browser smoke passed on `http://127.0.0.1:3010/projects`: existing local projects rendered in the UI, `17` projects were visible, and browser console reported no errors.
- 2026-06-13: plan 190 browser smoke passed on `http://127.0.0.1:3010/projects/project_mqavhwm7_875518fe/review`: source image loaded at `1672x941`, 7 asset previews loaded, Konva canvas rendered, there was no document horizontal overflow, and browser console reported no errors.
- 2026-06-13: plan 190 artifact discipline checks passed: `git diff --check` produced no output; tracked runtime artifact grep only matched `apps/slice-studio/storage/.gitkeep` and source files whose path contains `storage`.
- 2026-06-13: prelaunch codebase hardening plan 190 was created, executed, and archived to `docs/plans/completed/190-slice-studio-prelaunch-codebase-hardening.md`; facts used: Slice Studio checks/build pass, root workspace check passes, current local data lives under `apps/slice-studio/storage/`, and legacy/reference code remains present at root-level paths.
- 2026-06-13: concrete-analysis production launch planning completed from current docs and code facts: current API is open, project state is SQLite/filesystem-backed, no user ownership/session/payment entitlement exists, and current AI provider config is already OpenAI-compatible enough to support provider replacement planning.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed after Review Workbench i18n: TypeScript passed, Vitest 8 files / 55 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed after Review Workbench i18n: Next.js production build completed successfully.
- 2026-06-12: `git diff --check` passed after Review Workbench i18n.
- 2026-06-12: browser smoke passed on `http://127.0.0.1:3010/projects/project_mqavhwm7_875518fe/review`: default/fresh state rendered Chinese (`html lang=zh-CN`), Upload/Page/Asset/tool labels were Chinese, and topbar `scrollWidth == clientWidth` at 1280x720.
- 2026-06-12: browser smoke passed after switching to English: visible labels switched to English, `html lang=en`, canvas tool rail labels became `Select tool` / `Draw tool` / `Pan tool`, and topbar still had no hidden overflow at 1280x720.
- 2026-06-12: Asset Overview browser smoke passed after i18n: 7 cards rendered with 5 cards in the first row and five equal grid columns.
- 2026-06-12: Asset Overview browser smoke passed at 1280x720 after five-column change: first row rendered 5 cards, each about 231x212, with `grid-template-columns` resolving to five equal columns.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed after five-column overview change: TypeScript passed, Vitest 8 files / 55 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed after five-column overview change: Next.js production build completed successfully.
- 2026-06-12: `git diff --check` passed after five-column overview change.
- 2026-06-12: canvas tool rail browser smoke passed at 1280x720: top command bar no longer contained Select/Draw/Pan, `.canvasToolRail` rendered inside the stage at 46x128, and Select/Draw/Pan buttons exposed proper aria labels and active state.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed after canvas tool rail move: TypeScript passed, Vitest 8 files / 55 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed after canvas tool rail move: Next.js production build completed successfully.
- 2026-06-12: `git diff --check` passed after canvas tool rail move.
- 2026-06-12: inspector split browser smoke at 1280x720 passed: Assets panel height 446px, asset list height 295px, exactly 5 asset rows visible in the asset list, asset list `scrollHeight > clientHeight`, Details height 224px, and Details scrolls independently.
- 2026-06-12: selected-asset browser smoke passed: after clicking an asset, asset list still showed 5 rows, Details expanded to `scrollHeight=864` while keeping the same panel height, and the asset list remained independently scrollable.
- 2026-06-12: inspector split browser smoke at 2400x1350 passed: Assets panel height 480px, asset list height 324px, Details height 820px, and both asset list and details used their own scroll behavior as needed.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed after inspector split fix: TypeScript passed, Vitest 8 files / 55 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed after inspector split fix: Next.js production build completed successfully.
- 2026-06-12: `git diff --check` passed after inspector split fix.
- 2026-06-12: post-fix browser smoke at 1280x720 passed: topbar action `scrollWidth == clientWidth`, no hidden topbar controls, no document horizontal overflow, `stageMetaBar` absent, and only one Overview button remained.
- 2026-06-12: post-fix browser smoke at 1067x600, approximating 120% zoom pressure, passed: all topbar controls remained visible in the command row, no hidden topbar controls, no document horizontal overflow, and only one Overview button remained.
- 2026-06-12: Asset Overview browser smoke passed after fix: 7 asset cards rendered at stable 220x228 card boxes instead of stretching across the sparse grid.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed after zoom/layout fixes: TypeScript passed, Vitest 8 files / 55 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed after zoom/layout fixes: Next.js production build completed successfully.
- 2026-06-12: `git diff --check` passed after zoom/layout fixes.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed after plan 187 implementation: TypeScript passed, Vitest 8 files / 55 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed after plan 187 implementation: Next.js production build completed successfully.
- 2026-06-12: `git diff --check` passed after plan 187 implementation.
- 2026-06-12: browser smoke passed on `http://127.0.0.1:3010/projects/project_mqavhwm7_875518fe/review`; at 1280x720 there was no document horizontal overflow, topbar wrapped to two rows, and Pages/Stage/Inspector remained visible.
- 2026-06-12: browser smoke passed on the same page at 1672x941; topbar returned to one row, Pages/Stage/Inspector expanded proportionally, required controls were present, visible Review Workbench text had no Chinese copy, and browser console logs were empty.
- 2026-06-12: Figma MCP `get_design_context` and `get_screenshot` succeeded for file `rUcERiwtUnlb6ONy6xvrE5`, node `607:207`; screenshot size is 1672x941.
- 2026-06-12: concrete-analysis judgment: main contradiction is mapping a high-fidelity Figma workbench to existing live handlers/contracts without turning unsupported controls into fake functionality.
- 2026-06-12: implemented direct policy change by removing `tiny_dense_ui_text`; high-confidence tiny text is no longer forced to raster solely because the page is dense.
- 2026-06-12: `pnpm --dir apps/slice-studio exec vitest run tests/pencil-exporter.test.ts` passed: 19 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed: typecheck passed, Vitest 8 files / 55 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed: Next.js production build completed successfully.
- 2026-06-12: `git diff --check` passed.
- 2026-06-12: `project_mqavhwm7_875518fe` re-export passed; dense 09 v2 page changed to `sourceLineCount=205`, `textLayerCount=194`, `rasterPreservedTextCount=11`.
- 2026-06-12: Pencil MCP layout check for `project_mqavhwm7_875518fe` regenerated `design.pen` reported no layout problems.
- 2026-06-12: `project_mqar9qpo_93b911d9` re-export passed; page 1 changed to `sourceLineCount=219`, `textLayerCount=183`, `rasterPreservedTextCount=36`; page 2 remained `textLayerCount=84`, `rasterPreservedTextCount=0`.
- 2026-06-12: Pencil MCP layout check for `project_mqar9qpo_93b911d9` regenerated `design.pen` reported no layout problems.
- 2026-06-12: inspected `apps/slice-studio/server/text-reconstruction.ts`; `denseTextPage && located.bbox.height <= 10` returns `raster_preserve` with reason `tiny_dense_ui_text`.
- 2026-06-12: inspected `tmp/m29-debug/summary.json`; regenerated v2 dense PC image has 205 OCR lines, 189 M29 foreground matches, 72 editable text decisions, and 129 `tiny_dense_ui_text` decisions.
- 2026-06-12: temporary `tmp/upscalerjs-probe` install confirmed UpscalerJS can initialize with `@tensorflow/tfjs-node` on this machine; no mainline dependency changed.
- 2026-06-12: `pnpm --dir apps/slice-studio exec vitest run tests/pencil-exporter.test.ts` passed: 18 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed: typecheck passed, Vitest 8 files / 54 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed: Next.js production build completed successfully.
- 2026-06-12: `POST /api/projects/project_mqar9qpo_93b911d9/export-project` passed with `assetCount=36`, `pageCount=2`.
- 2026-06-12: regenerated manifest page 1 reported `sourceLineCount=219`, `textLayerCount=116`, `rasterPreservedTextCount=103`, `skippedTextCount=0`.
- 2026-06-12: regenerated manifest page 2 reported `sourceLineCount=84`, `textLayerCount=84`, `rasterPreservedTextCount=0`.
- 2026-06-12: Pencil MCP screenshot of regenerated `page_0001__frame` inspected; dense OCR text pollution was removed.
- 2026-06-12: structured analysis completed; current main contradiction is missing text/raster ownership arbitration, not OCR provider availability.
- 2026-06-12: `git diff --check` passed.
- 2026-06-12: targeted stale-mainline grep passed for current docs; remaining matches are negations, historical/reference runbooks, archived plans, or old completed-plan evidence.
- 2026-06-12: `pnpm --dir apps/slice-studio run check` passed: typecheck passed, Vitest 8 files / 52 tests passed.
- 2026-06-12: `pnpm --dir apps/slice-studio run build` passed: Next.js production build completed successfully.
- 2026-06-12: scoped documentation commit created; unrelated AI prompt code/test changes remain unstaged.

## Failure attempt ledger
- None recorded.

## User input needed
- None recorded.

## Last checkpoint
- 2026-06-14 08:06 CST: plan 193 P9 no-vector text cleanup is validated against a restarted current-code full export for `project_mqc1wpkd_123c88b0`. The new P9 manifest exports `待支付` as white, the bottom payment knockout covers the OCR/physical union, P9 key text regions have `stillFgRatio=0` in the remainder pixel scan, Pencil `page_0009__frame` renders without the previous visible ghosting, and `pnpm run check`, `pnpm run build`, and `git diff --check` passed. Dev server is running on 3010/4110/4120 from the restarted current code.
