# Figma Plugin Directory

Current product work belongs in:

```text
apps/slice-studio/
```

This plugin is a deferred runtime asset from the older Draft/plugin route. Slice Studio now exports `project.zip/design.pen` from saved slices instead of using this plugin as the default delivery path.

Before modifying or reviving this plugin path, read:

```text
docs/engineering/current-code-map.md
docs/engineering/legacy-code-inventory.md
```

Do not fix current Slice Studio upload, AI slicing, storage, or export behavior in this directory unless a new active plan explicitly changes the product route.
